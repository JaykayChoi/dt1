# assets

Phase 11 랩(`examples/openusd-lab`)에서 생성한 팹 USD 씬을 여기로 복사한다.

- 예: `fab_layout.usda` (+ 참조 파일 `oht_asset.usda` 를 같이 복사)
- 프림 이름에 `OHT` / `AGV` 가 포함돼 있어야 `dt1.fabsim.viz` 확장이 반송 장비로 인식한다.
  Phase 11 랩은 이미 `/World/Fab/OHT_01`·`/World/Fab/OHT_02`·`/World/Fab/AGV_01`·`/World/Fab/AGV_02`
  를 심어 둔다.

실제 `.usda` 는 이 저장소에 두지 않는다 — 각자 Phase 11 산출물을 복사해 사용한다.
